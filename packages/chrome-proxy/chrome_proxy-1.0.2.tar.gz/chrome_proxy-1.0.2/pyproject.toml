[project]
name = "chrome-proxy"
version = "1.0.2"
description = "chrome remote debug proxy"
authors = [
    { name = "funcdefmain", email = "741132517@qq.com" },
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
chrome-proxy = "chrome_proxy.chrome_manager:main"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
