[project]
name = "grapevne"
version = "0.1.0"
description = "Python library for grapevne snakemake support"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "packaging>=24.1",
]

[tool.uv]
dev-dependencies = [
    "nox>=2024.4.15",
    "pytest>=8.3.2",
    "ruff>=0.6.3",
]
