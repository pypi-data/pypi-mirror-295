function switchMode(mode) {
    setTimeout(function () {
        if (location.pathname == "/") {
            location.href = "/mode/" + mode;
            return;
        }
        location.href = "/mode/" + mode + location.pathname;
    }, 300)
}


$(document).ready(function () { $("#tableData").DataTable({ paging: !1, language: { info: "_TOTAL_ items" }, columnDefs: [{ targets: 0, orderable: !1 }, { targets: 4, orderable: !1 }, { orderSequence: ["desc", "asc"], targets: [1] }], order: [[1, "asc"]] }) }); var inputs = document.querySelectorAll(".uploadFile"); Array.prototype.forEach.call(inputs, function (e) { var t = e.nextElementSibling, n = t.innerHTML; e.addEventListener("change", function (e) { var a = ""; a = this.files && this.files.length > 1 ? (this.getAttribute("data-multiple-caption") || "").replace("{count}", this.files.length) : e.target.value.split("\\").pop(), a ? t.querySelector("span").innerHTML = a : t.innerHTML = n }) });