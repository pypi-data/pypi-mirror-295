[project]
name = "libbrainz"
version = "0.1.0"
description = "ListenBrainz API client"
authors = [
    { name = "cătălin", email = "catalin@roboces.dev" },
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.2",
    "pydantic>=2.9.1",
    "pydantic-settings>=2.5.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
