from setuptools import setup, find_packages

setup(
    name='stackerd-game',
    version='0.1.0',
    description='An alternative to Pygame for game development',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        # List any dependencies your package needs
    ],
    python_requires='>=3.6',
)
