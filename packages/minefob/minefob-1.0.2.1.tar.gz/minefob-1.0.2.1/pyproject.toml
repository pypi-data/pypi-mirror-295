[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "minefob"
version = "1.0.2.1"
authors = [{ name = "Caleb North", email = "contact@fivesixfive.dev" }]
description = "(Deprecated) Module to control Minehut servers via Python"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.6"