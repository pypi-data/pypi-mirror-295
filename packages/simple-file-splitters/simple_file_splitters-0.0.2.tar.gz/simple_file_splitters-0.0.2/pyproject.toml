[project]
name = "simple_file_splitters"
version = "0.0.2"
authors = [
  {name="Jonas Fuchß"}
]
dependencies = [
  "pandas",
  "langchain_text_splitters",
  "langchain_community",
  "docx2python",
  "PyMuPDF"
]
description = "A small package to quickly read, process and recursively split multiple different, text-based filetypes (like .txt, .pdf, .docx) into overlapping chunks of predefined sizes."
readme = "README.md"
requires-python = ">=3.12.0"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Mozilla Public License 2.0 (MPL 2.0)",
    "Operating System :: OS Independent",
]
