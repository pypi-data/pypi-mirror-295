[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python_web_search"
version = "0.4"
requires-python = ">= 3.8"
readme = "README.md"

[project.scripts]
search = "python_web_search.main:main"