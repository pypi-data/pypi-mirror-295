[tool.poetry]
name = "sqlalchemy-mock"
version = "1.1.0"
description = "The package for working with SQLAlchemy in unit tests"
authors = ["ivanostapiuk <ost.ivan13@gmail.com>"]
readme = "README.md"
packages = [{include = "sqlalchemy_mock"}]

[tool.poetry.dependencies]
python = "^3.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project.urls]
Homepage = "https://github.com/ivanostapiuk/sqlalchemy-mock"
