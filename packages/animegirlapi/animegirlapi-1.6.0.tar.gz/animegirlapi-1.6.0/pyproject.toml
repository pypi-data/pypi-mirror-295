[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "animegirlapi"
version = "1.6.0"
description = "An API that can output anime girls, and is 100% manually verified"
authors = ["Allexx <allexx129@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]
pytest = "^6.2.5"

[tool.poetry.scripts]
# Define any command-line scripts your package might have here
