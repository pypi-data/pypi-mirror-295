[tool.poetry]
name = "useful_ts_tools"
version = "0.1.2"
description = "Utilities to generate useful features for time series"
authors = ["fwo"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10"
pandas = ">=2.1.1"
numpy = "^1.26.4"
holidays = "^0.55"

[tool.poetry.group.test.dependencies]
pytest = "^8.3.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
