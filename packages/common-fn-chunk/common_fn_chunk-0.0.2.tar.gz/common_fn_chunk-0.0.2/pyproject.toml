[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "common-fn-chunk"
dynamic = ["version"]
description = ''
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = []
authors = [
  { name = "Hao Wu", email = "haowu@dataset.sh" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.8",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = []

[project.urls]
Documentation = "https://github.com/Hao Wu/common-fn-chunk#readme"
Issues = "https://github.com/Hao Wu/common-fn-chunk/issues"
Source = "https://github.com/Hao Wu/common-fn-chunk"

[tool.hatch.version]
path = "src/common_fn_chunk/__about__.py"

[tool.hatch.envs.types]
extra-dependencies = [
  "mypy>=1.0.0",
]
[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive {args:src/common_fn_chunk tests}"

[tool.coverage.run]
source_pkgs = ["common_fn_chunk", "tests"]
branch = true
parallel = true
omit = [
  "src/common_fn_chunk/__about__.py",
]

[tool.coverage.paths]
common_fn_chunk = ["src/common_fn_chunk", "*/common-fn-chunk/src/common_fn_chunk"]
tests = ["tests", "*/common-fn-chunk/tests"]

[tool.coverage.report]
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]
